{% extends 'base.html.twig' %}

{% block title %}Se Connectée{% endblock %}

{% block body %}
<div class="container">
    <div class="login__form mt3">
        <form method="post" class="stack-large">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}


            <h1 class="login__title">Se connecter</h1>
         <div class="form-group"> <label for="inputName">Nom d'utilisateur</label>
            <input type="text" value="{{ last_username }}" name="name" id="inputName" class="form-control" autocomplete="username" required autofocus>
         </div>
            <div class="form-group">
            <label for="inputPassword">Mot de passe</label>
            <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
            </div>

            <input type="hidden" name="_csrf_token"
                   value="{{ csrf_token('authenticate') }}"
            >
<div class="grid2">

    <button class="btn-primary btn-block" type="submit">
        Se Connecter
    </button>
</div>

        </form>
    </div>


</div>
{% endblock %}
